/* Re-export del Context Engine compartido — fuente única en
 * supabase/functions/_shared/intelligence/context.ts (app + Edge Functions).
 * El resumen compacto por periodo que alimenta la capa de IA. */
export * from '../../supabase/functions/_shared/intelligence/context'
